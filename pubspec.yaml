name: flutter_production_test
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  video_player: ^2.11.1
  go_router: ^17.3.0
  http: ^1.6.0
  dio: ^5.10.0
  intl: ^0.20.3
  currency_text_input_formatter: ^2.3.1
  shimmer: ^3.0.0
  flutter_riverpod: ^3.0.3
  riverpod_annotation: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^3.0.3
  build_runner: ^2.5.4

flutter:
  uses-material-design: true

dependency_overrides:
  intl: ^0.20.3